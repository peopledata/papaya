//SPDX-License-Identifier: UNLICENSED

// Solidity files have to start with this pragma.
// It will be used by the Solidity compiler to validate its version.
pragma solidity ^0.8.9;

import "hardhat/console.sol";

// This Smart Contract is cerificate each datoms when it created.

// When a new datom created by datomspace, its root node hash and selected meta data
// will be used to mint a NFT token.


// "One datom maps to one NFT token" 

